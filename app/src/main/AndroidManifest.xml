<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.usuario.ad_ejercicios_t3"
    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".PartesActivity">
        </activity>
        <activity android:name=".NotasActivity">
        </activity>
        <activity android:name=".NoticiasActivity">
        </activity>
        <activity android:name=".TitularesActivity">
        </activity>
        <activity android:name=".CreacionActivity">
        </activity>
    </application>

</manifest>